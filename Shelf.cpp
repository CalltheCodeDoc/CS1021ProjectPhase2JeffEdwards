#include "shelf.h"
/*
Shelf::Shelf() {
    num_customers = 0;
}

void Shelf::addMedia(Customer* person) {
    if (num_customers >= MAX_SIZE) {
        throw FullShelf();
    }
    movies[num_movies] = movie;
    num_customers++;
}

Movie* Shelf::removeMovie() {
    if (num_customers <= 0) {
        throw EmptyShelf();
    }
    num_customers--;
    return movies[num_customers];
}

int Shelf::getNumMovies() {
    return num_customers;
}
*/